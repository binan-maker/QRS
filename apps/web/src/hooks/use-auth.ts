"use client";

/**
 * Re-export of useAuth from the AuthContext.
 * Provides a canonical import path: import { useAuth } from "@/hooks/use-auth"
 */

export { useAuth } from "@/contexts/auth-context";
