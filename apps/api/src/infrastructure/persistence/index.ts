/**
 * @infrastructure/persistence — Drizzle ORM repository implementations
 *
 * Phase 3: implement IQrCodeRepository, IUserRepository, etc. here.
 * Each class takes a Drizzle db instance and implements the domain interface.
 *
 * Example structure (to be implemented in Phase 3):
 *
 *   export class DrizzleUserRepository implements IUserRepository {
 *     constructor(private readonly db: DrizzleDb) {}
 *     async findById(id: string) { ... }
 *     ...
 *   }
 */

// Placeholder — implementations added in Phase 3.
export {};
