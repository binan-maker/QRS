/**
 * @binro/core — Shared entity models
 *
 * These are the canonical domain model types shared across mobile, web, and API.
 * They are framework-agnostic and have zero runtime dependencies.
 *
 * Currently re-exports from ../types/ while the types are progressively
 * moved to value-objects and models during Phase 1–3 of the migration.
 */

export * from "../types/qr";
export * from "../types/user";
export * from "../types/trust";
