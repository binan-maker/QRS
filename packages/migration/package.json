{
  "name": "@binro/migration",
  "version": "1.0.0",
  "private": true,
  "description": "Auth-only user import into Supabase Auth",
  "scripts": {
    "migrate": "tsx data/migrate-auth.ts",
    "migrate:dry": "MIGRATION_DRY_RUN=true tsx data/migrate-auth.ts"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.109.0",
    "dotenv": "^18.0.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
