/**
 * @binro/ui — Shared design system
 *
 * Phase 1: design tokens only (colours, typography, spacing).
 * Phase 4: shared components usable in both React Native and web.
 *
 * Zero runtime dependencies at this stage.
 */

export * from "./tokens/index";
