{
  "name": "@binro/db",
  "version": "0.0.1",
  "private": true,
  "description": "Drizzle ORM schema, migrations, and PostgreSQL client factory.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "db:generate": "drizzle-kit generate --config=drizzle.config.ts",
    "db:push": "drizzle-kit push --config=drizzle.config.ts",
    "db:studio": "drizzle-kit studio --config=drizzle.config.ts"
  },
  "peerDependencies": {
    "drizzle-orm": ">=0.39.0",
    "pg": ">=8.0.0",
    "zod": ">=3.0.0"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.10",
    "typescript": "~5.9.2"
  }
}
